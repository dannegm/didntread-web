import IconWrapper, { type IconProps } from './icon-wrapper';

export default function {{pascalCase name}}(props: IconProps) {
    return (
        <IconWrapper {...props}>

        </IconWrapper>
    );
}
